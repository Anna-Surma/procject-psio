#ifndef PLATFORM_H
#define PLATFORM_H

#include "game.h"

class Platform
{
public:
    Platform();
    ~Platform();
    std::vector<sf::Sprite> create_spirit(sf::Texture &texture);

private:
    sf::Vector2u platform_pos[20];
};
#endif // PLATFORM_H
